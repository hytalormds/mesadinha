import { StyleSheet } from "react-native";

export default StyleSheet.create({
  headerBackButton: {
    flexDirection: "row",
    alignItems: "center",
  },

  headerBackText: {
    color: "#007BFF",
    fontSize: 16,
  },
});
