import { StyleSheet } from "react-native";

export default StyleSheet.create({
  headerBackButton: {
    flexDirection: "row",
    alignItems: "center",
    paddingHorizontal: 8,
  },

  headerBackText: {
    color: "#007BFF",
    fontSize: 16,
    fontWeight: "600",
  },
});